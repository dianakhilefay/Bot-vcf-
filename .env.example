# Variables d'environnement pour le Bot WhatsApp VCF

# Nom du bot
BOT_NAME=VCF Bot

# Version du bot  
BOT_VERSION=1.0.0

# Clé API pour authentifier les appels depuis l'API externe
# 👆 Doit être la même que dans l'API
BOT_API_KEY=VCF2025_DYBY_SECRET_API_KEY_789xyz

# Port d'écoute (Heroku utilise la variable PORT automatiquement)
PORT=8000

# Mode de fonctionnement
NODE_ENV=production

# Optionnel: Préfixe par défaut pour les commandes
DEFAULT_PREFIX=.

# Optionnel: Nombre maximum de sessions simultanées
MAX_SESSIONS=10
